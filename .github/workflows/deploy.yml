name: Build and Deploy

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Build Application
        run: pnpm build
        env:
          # This line maps the GitHub Secret to your build environment
          JWT_SECRET: ${{ secrets.JWT_SECRET }}
          # If you have other variables like DB_URL, add them here too:
          # DATABASE_URL: ${{ secrets.DATABASE_URL }}

      # Add your deployment steps here (e.g., SCP, SSH, etc.)
